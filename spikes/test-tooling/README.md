# Test tooling spike

**Goal:** Prove Vitest runs in the workspace and Playwright can launch Chromium to load a data URL.

**Note:** Requires Playwright browser binaries (`npx playwright install chromium`).

**Test:** `src/vitest-playwright.test.ts`
