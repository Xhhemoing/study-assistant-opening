import { PlannedPage } from "../../../../features/workspace/planned-page";

export default function ExportPage() {
  return <PlannedPage title="数据导出" description="可迁移备份和导出报告将在后续数据可携带性切片中接入。当前可以在设置中查看诊断信息。" />;
}
