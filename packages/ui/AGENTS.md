# @aistudy/ui

共享 UI 组件库(React + Tailwind)。app-shell/card/drawer/field 等通用组件所有者。

- 仅 Tailwind 工具类,禁止原生 CSS 与内联 style(globals.css 为遗留)。
- 图标用 lucide-react;保留可访问性状态。
- 验证:`npm run typecheck -w @aistudy/ui` + 组件 `*.test.tsx`。
