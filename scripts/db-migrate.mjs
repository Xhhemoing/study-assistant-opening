#!/usr/bin/env node
/**
 * Placeholder migration runner for Phase 0.
 * Schema migrations land with the database package in a later task.
 */
console.log(
  JSON.stringify({
    status: "ok",
    message: "No migrations registered yet; platform schema task pending.",
  }),
);
