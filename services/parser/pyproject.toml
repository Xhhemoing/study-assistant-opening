[project]
name = "opening-parser"
version = "0.1.0"
description = "Offline Docling parser for opening materials"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]
